cmake_minimum_required(VERSION 3.16...3.28 FATAL_ERROR)

AddExe(multiple_lights_desert glfw.icns light.cpp)

target_link_libraries(multiple_lights_desert PRIVATE stbimage)

target_include_directories(multiple_lights_desert PRIVATE ${CMAKE_SOURCE_DIR}/deps ${CMAKE_SOURCE_DIR}/StbImage)

add_custom_command(
    TARGET multiple_lights_desert POST_BUILD
    COMMAND ${CMAKE_COMMAND} -E copy_directory
    ${CMAKE_CURRENT_SOURCE_DIR}/resources/
    ${CMAKE_CURRENT_BINARY_DIR}/multiple_lights_desert.app/Contents/Resources
)