cmake_minimum_required(VERSION 3.16...3.28 FATAL_ERROR)

AddExe(anti_aliasing_msaa glfw.icns anti_aliasing_msaa.cpp)

target_link_libraries(anti_aliasing_msaa PRIVATE stbimage)

target_include_directories(anti_aliasing_msaa PRIVATE ${CMAKE_SOURCE_DIR}/deps ${CMAKE_SOURCE_DIR}/StbImage)

add_custom_command(
    TARGET anti_aliasing_msaa POST_BUILD
    COMMAND ${CMAKE_COMMAND} -E copy_directory
    ${CMAKE_CURRENT_SOURCE_DIR}/resources/
    ${CMAKE_CURRENT_BINARY_DIR}/anti_aliasing_msaa.app/Contents/Resources
)

